{
  "version": 2,
  "name": "tsla-analytics-dashboards",
  
  "buildCommand": "pip install -r requirements.txt",
  "outputDirectory": ".",
  "installCommand": "pip install -r requirements.txt",
  
  "builds": [
    {
      "src": "api/wsgi.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "50mb",
        "runtime": "python3.11",
        "pythonVersion": "3.11",
        "wsgiHandler": "api.wsgi:app"
      }
    }
  ],
  
  "routes": [
    {
      "src": "/api/health",
      "dest": "api/handler.py"
    },
    {
      "src": "/api/(.*)",
      "dest": "api/handler.py",
      "methods": ["GET", "POST", "PUT", "DELETE", "OPTIONS"]
    },
    {
      "src": "/(.*)",
      "dest": "api/wsgi.py"
    }
  ],
  
  "env": {
    "FLASK_ENV": "production",
    "DEBUG": "false"
  },
  
  "regions": ["iad1"],
  
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-cache, no-store, must-revalidate"
        },
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        }
      ]
    }
  ]
}
